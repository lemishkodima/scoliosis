# Wow Design Redesign Plan

## Мета

Перетворити поточний прототип із "сайту з гарним відео" на преміальний бренд-досвід для професійної асоціації: сильний перший екран, кінематографічне завантаження, глибина при скролі, чисті фірмові мотиви та відчуття статусності членства.

## Основні принципи

- Hero має працювати як сцена, а не як звичайний блок із фоновим відео.
- Loader має відкривати сайт через планки/вікно, швидко і впевнено.
- Перший контентний блок має накладатися поверх hero як преміальна панель.
- Анімації мають бути архітектурними: маски, reveal, depth, control, без хаотичних рухів.
- Логотип із круговим текстом використовується як фірмовий системний мотив.
- Курсор, header і secondary effects не повинні перебивати головний фокус.

## Етапи

### Етап 0. Image Performance Hotfix

Статус: done

Задачі:
- [x] Конвертувати важкі PNG-фото у WebP.
- [x] Перемкнути HTML на WebP-версії.
- [x] Прибрати PNG-оригінали з git, щоб деплой не тягнув сотні мегабайт.
- [x] Додати `width`/`height` для зображень, щоб зменшити layout shift.

Результат:
- Важкі фото зменшені з 16-28 MB до приблизно 56-394 KB.
- Логотипи зменшені до 12-44 KB.
- Сайт більше не має активних посилань на PNG-зображення.

### Етап 0.1. Mobile Interaction Polish

Статус: done

Задачі:
- [x] Вирівняти логотип по центру в мобільному header.
- [x] Додати швидку scramble-анімацію для фраз `Більше знань...`.
- [x] Зробити мобільну кнопку `Подати заявку` круглою fixed CTA у правому нижньому куті.
- [x] Перебудувати `Три прості кроки` у карту з route-line і checkpoint-маркерами.
- [x] Продовжити штрихований фон через усю сторінку на мобільному.

Результат:
- Мобільний header тепер центрує знак бренду.
- CTA на мобільному залишається доступним поверх сторінки і має pulse-анімацію.
- Open-lines текст збирається швидким перебором символів при вході у viewport.
- Steps-блок виглядає як маршрутна карта з чекпоінтами.

### Етап 0.2. Native Scroll Only

Статус: done

Задачі:
- [x] Прибрати самописний smooth-scroll і Locomotive Scroll, залишити нативний скрол браузера.
- [x] Прибрати scroll-driven depth/parallax модуль.
- [x] Прибрати `data-parallax` атрибути з контентних блоків.

Результат:
- Desktop і mobile scroll працюють нативно без JS-scroll engine і без CSS `scroll-behavior: smooth`.
- Scroll-driven parallax більше не виконується, щоб уникнути ривків під час прокрутки.

### Етап 0.3. Mobile Hero Visibility

Статус: done

Задачі:
- [x] Збільшити видиму висоту hero-video на мобільному.
- [x] Прибрати власний фон, рамку, blur і shadow у hero CTA-блоку.
- [x] Оновити cache-bust версію CSS/JS.

Результат:
- На viewport `430x932` hero-video має висоту `482px` замість попередніх `340px`.
- CTA, lead і stats у hero більше не сидять у власній картці, а накладаються прямо поверх cinematic-сцени.
- Browser QA: `.hero-dock` має прозорий фон, `0px` border, `none` shadow і `none` backdrop-filter.

### Етап 1. Loader + Hero + First Overlay

Статус: done

Задачі:
- [x] Скоротити loader і зробити появу більш premium.
- [x] Посилити ефект планок, які відкривають контент як вікно.
- [x] Перебудувати hero у cinematic scene з глибиною відео.
- [x] Об'єднати CTA, lead і напрями в акуратний control dock.
- [x] Зробити intro-блок виразним overlay-шаром поверх hero.

Результат:
- Loader тепер коротший, з більш виразними планками та швидшим відкриттям контенту.
- Hero отримав depth-площини, сильніші маски, light sweep перед повтором відео.
- CTA, lead і напрями об'єднані в один glass control dock.
- Intro-блок піднятий поверх hero як окремий overlay-шар.

### Етап 1.1. Monochrome Loader Panels

Статус: done

Задачі:
- [x] Змінити плити loader на монохромний темно-синій колір із footer-палітри.
- [x] Прибрати cyan/lime домішки з плит.
- [x] Зробити букви в loader-лого світлими й читабельними.
- [x] Прибрати світлу щілину між плитами через overlap і слабші внутрішні лінії.
- [x] Перебудувати плити у ступінчасту композицію, ближчу до референсу з блоками навколо центру.
- [x] Замінити колонкове відкриття на top/bottom плити, які з'їжджають угору й униз як горизонтальне вікно.
- [x] Збільшити кількість плит до 24 і зробити reveal від центральної осі до країв.

Результат:
- Loader відкривається через темно-сині блоки різної висоти, без сторонніх кольорових домішок.
- Назва `UKRAINIAN SCOLIOSIS ASSOCIATION` читається світлим кольором без зайвої плашки.
- Browser QA: loader стартує як темно-сині плити, reveal відкриває сторінку від центру через часті вертикальні планки, після reveal сторінка не має JS-помилок.

### Етап 2. Header + Cursor Polish

Статус: done

Задачі:
- [x] Зробити header максимально легким на старті.
- [x] Посилити glass-state тільки після скролу.
- [x] Зменшити конфлікт курсору з текстом.
- [x] Додати magnetic/soft hover тільки для інтерактивних елементів.

Результат:
- Header на старті став тихішим, а після скролу отримує чіткіший glass-state.
- Навігація отримала тонкий underline-hover замість важких ефектів.
- Курсор отримав режим читання над текстом і більше не має агресивно перекривати заголовки.

### Етап 3. Section Animation System

Статус: done

Задачі:
- [x] Замінити простий fade-up на mask/clip reveal.
- [x] Додати stagger для карток.
- [x] Додати reveal для фото через вертикальну маску.
- [x] Зробити секції більш шаровими, щоб скрол відчувався як рух камери.

Результат:
- Reveal-система тепер застосовується до section heading, media-блоків, галереї, кроків і форми.
- Фото проявляються через vertical mask із легким scale-down.
- Картки й секції мають stagger через `--reveal-index`.

### Етап 4. Image Art Direction

Статус: done

Задачі:
- [x] Відібрати зображення, які напряму пов'язані зі спиною, сколіозом, терапією, ортезуванням.
- [x] Уніфікувати crop, contrast, blue medical grading.
- [x] Прибрати відчуття випадкового набору stock-зображень.

Результат:
- Для mission, audience і gallery додано єдиний холодний medical grading.
- Фото отримали overlay-шари, контрольований contrast/saturation і більш керовані crop-и.
- Медіа-блоки стали ближчими до editorial medical identity, а не до випадкового набору картинок.

### Етап 5. Final Visual QA

Статус: done

Задачі:
- [x] Перевірити desktop hero на 1280x720 viewport.
- [x] Перевірити mobile hero на 390x844 viewport.
- [x] Перевірити плавність скролу на рівні DOM/console без помилок.
- [x] Перевірити, що loader не затримує користувача надто довго.
- [x] Перевірити, що CTA видно без прокрутки.

Результат QA:
- Desktop 1280x720: hero dock повністю вміщується, stats не обрізані, horizontal overflow немає.
- Mobile 390x844: кнопки hero в один ряд, dock повністю вміщується, horizontal overflow немає.
- Console: помилок і warning після перевірки не зафіксовано.
- Сервер відповідає на `http://localhost:4173/`.
